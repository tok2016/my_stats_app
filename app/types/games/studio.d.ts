export interface RawgStudio {
  id: number;
  name: string;
  slug: string;
  games_count: number;
}

export interface IgdbInvolvedStudio {
  id: number;
  developer: boolean;
  publisher: boolean;
  company: number;
}
